export default authHeader =() => {
    // get the current user user const user = JSON.parse(localStorage.getItem('user'));
    // check if the user exist and return the token {'x-access-token': user.accessToken}
    // if failed return as empty object {}
    

}